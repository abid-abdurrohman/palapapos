<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.palapapos">

    <uses-permission android:name="android.permission.INTERNET" />
    android:hardwareAccelerated = "true"
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splashscreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Dashboard"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Nasional"
            android:label="@string/title_activity_nasional"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NasionalTerkini" />
        <activity android:name=".NasionalTerpopuler" />
        <activity
            android:name=".Terkini"
            android:label="@string/title_activity_terkini"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Terpopuler"
            android:label="@string/title_activity_terpopuler"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>